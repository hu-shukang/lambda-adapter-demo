generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-1.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tag {
  id         String   @id
  category   String
  name       String
  updateTime DateTime
  updateUser String

  @@index([category, updateTime], name: "idx_category_updatetime")
}

model Organization {
  id            String             @id
  name          String
  updateTime    DateTime
  updateUser    String
  parent        Organization?      @relation("OrganizationToOrganization", fields: [parentId], references: [id])
  children      Organization[]     @relation("OrganizationToOrganization")
  parentId      String?
  organizations UserOrganization[]
}

model User {
  id                String             @id
  email             String             @unique
  name              String
  status            String
  enterDay          DateTime
  cognitoUserStatus String
  updateTime        DateTime
  updateUser        String
  organizations     UserOrganization[]
}

model UserOrganization {
  user           User         @relation(fields: [userId], references: [id])
  userId         String
  organization   Organization @relation(fields: [organizationId], references: [id])
  organizationId String
  position       String
  updateTime     DateTime
  updateUser     String

  @@id([userId, organizationId])
  @@index([organizationId])
}
