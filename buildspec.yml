version: 0.2

phases:
  install:
    commands:
      - npm ci
      - npm run prisma:generate
      - mkdir artifact
  pre_build:
    commands:
      - npm run lint
      - echo Logging in to Amazon ECR
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin ${ECR_REPOSITORY_URI}
      - IMAGE_TAG=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - echo "IMAGE_TAG:${IMAGE_TAG}"
  build:
    commands:
      - TIMESTAMP=$(date +%Y%m%d%H%M%S)
      - npm run build
      # 打包静态文件
      - echo "Upload Static Files..."
      - aws s3 sync build/client s3://$WEB_BUCKET/ --delete --acl bucket-owner-full-control
      # Build Lambda and layer
      - echo "Build Lambda And Layer..."
      - cd ${CODEBUILD_SRC_DIR}/lambda/common-layer/nodejs
      - npm ci --omit=dev
      - cd ${CODEBUILD_SRC_DIR}/lambda/prisma-layer/nodejs
      - npm ci --omit=dev
      - mkdir prisma
      - cp ${CODEBUILD_SRC_DIR}/prisma/schema.prisma ${CODEBUILD_SRC_DIR}/lambda/prisma-layer/nodejs/prisma/schema.prisma
      - npx prisma db pull
      - npx prisma generate --no-engine
      - cd ${CODEBUILD_SRC_DIR}
      - npm run build:lambda
      - chmod +x package-lambdas.sh
      - bash package-lambdas.sh $ASSET_BUCKET $TIMESTAMP
      # Build Docker Image
      - echo "Building the Docker image..."
      - cd ${CODEBUILD_SRC_DIR}
      - docker build -t ${ECR_REPOSITORY_URI}:latest .
      - docker tag ${ECR_REPOSITORY_URI}:latest ${ECR_REPOSITORY_URI}:${IMAGE_TAG}
      - docker image ls
      - docker push ${ECR_REPOSITORY_URI}:${IMAGE_TAG}
      # cdk -> cloudformation
      - echo "CDK Synth..."
      - npx cdk synth LambdaStack-$ENV --require-approval never -c env=$ENV -c timestamp=$TIMESTAMP -c imageTag=$IMAGE_TAG > ${CODEBUILD_SRC_DIR}/artifact/${SYNTH_TEMPLETE}

artifacts:
  files:
    - ${SYNTH_TEMPLETE}
  base-directory: artifact
